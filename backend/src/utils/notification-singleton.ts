/**
 * Notification Service Singleton
 * 
 * WHY: Re-export notification service singleton for convenient imports
 * Provides access to the initialized NotificationService instance
 */

export { getNotificationService } from '@/services/notification.service';
